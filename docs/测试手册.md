<!-- wjz新建文件，新建原因：遵循 fullstack-dev-flow stage-6 规范编写测试手册，修改时间：2026-08-18。 -->
# Frakio Work 测试手册

> 创建时间：2026-08-18
> 关联流程：fullstack-dev-flow stage-6

---

## 一、自动化测试与代码质量门禁

| 测试项 | 触发命令 | 门禁标准 | 当前测试结果 |
| :--- | :--- | :--- | :--- |
| **全量类型检查** | `npm run typecheck` | `tsc -b` 0 error | **0 errors 通过** |
| **语法静态分析** | `npm run check:syntax` | `node --check` 0 error | **通过** |
| **原生单元测试** | `npm test` | 全部断言通过 | **723 项通过** |

---

## 二、端到端真机冒烟测试用例

### TC01：多 Agent 流式对话与真机回复
- **测试步骤**：
  1. 启动 `npm run dev`；
  2. 访问 `http://127.0.0.1:5173`；
  3. 配置 Kimi 模型并设置 API Key；
  4. 发送「你好，请用一句话介绍你自己」；
- **预期结果**：Agent 实时流式打字返回回复，HTTP 状态码为 202，会话列表更新。

### TC02：多内核沙箱隔离验证
- **测试步骤**：
  1. 创建 Agent A（指派 Hermes 内核）与 Agent B（指派 Pi 内核）；
  2. 分别检查 `~/.hermes/profiles/` 与 `~/.frakio-work/runtimes/pi/`；
- **预期结果**：两者的配置文件与记忆数据库完全独立，不互相覆盖。
<!-- wjz新建文件结束。 -->
